#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
//
 if (argc != 2)
 {
    printf("Usage: ./recover IMAGE\n");
    return 1;
 }
  // remember filename
    char *memory_card = argv[2];
}